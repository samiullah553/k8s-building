kind: persistentVolumeClaim
apiVersion: v1
metadata:
  name: local-pvc
  labels: local-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests: 
      storage: 1Gi
  storageClassName: local-storage

kind: persistentVolume
apiVersion: v1
metadata:
  name: local-pv
  labels:
    app=locals

spec:
  capicity: 
    storage: 1Gi
  accessModes: 
    - ReadWriteOnce
  persistenVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /mnt/data
  
